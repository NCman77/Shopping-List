{
  "name": "shopping-list",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:e2e": "playwright test",
    "test:rules": "npx --yes firebase-tools@15.30.1 emulators:exec --only firestore --project demo-shopping-list \"node --test tests/integration/firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@playwright/test": "^1.55.0",
    "firebase": "^11.6.1"
  }
}
